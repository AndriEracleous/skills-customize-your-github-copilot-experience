# 📘 Assignment: Building REST APIs with FastAPI

## 🎯 Objective

Build a small REST API using FastAPI that supports basic CRUD operations and returns clean JSON responses.

## 📝 Tasks

### 🛠️	Design the API and Data Model

#### Description
Define the resource your API will manage and plan the endpoints you will implement.

#### Requirements
Completed program should:

- Choose one resource (example: books, movies, tasks)
- Define the data fields for that resource (example: title, author, year)
- List the endpoints you will build (example: create, list, get by id)


### 🛠️	Implement and Test the API

#### Description
Build the FastAPI app with the planned endpoints and verify it works with sample requests.

#### Requirements
Completed program should:

- Implement endpoints for create and list operations
- Include an endpoint to get a single item by id
- Return JSON responses with appropriate status codes
- Validate request data using Pydantic models
- Demonstrate testing with at least two sample requests and responses
